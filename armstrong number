// User function Template for Java
class Solution {
    static boolean armstrongNumber(int n) {
        // code here
        int  m = n;
        int s=0;
        while(n!=0){
            s += Math.pow( n%10,3);
            n = n/10;
            
        }
        return s==m;
    }
}
